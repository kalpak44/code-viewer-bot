dist
out
coverage
node_modules
package-lock.json

# Generated by homelab-infra and pushed verbatim by `just deploy github code-viewer-bot`.
# Formatting them here cannot stick: the next apply restores its own copy, and format:check
# would then fail on files no one can fix in this repo, blocking every merge.
.github

# A licence is quoted text, not source.
LICENSE.md
